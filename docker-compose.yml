services:

  mysql-pagamentos:
    image: mysql:8.0
    container_name: mysql-pagamentos
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-changeme}
      MYSQL_DATABASE: alurafood-pagamento
    ports:
      - "3306:3306"
    volumes:
      - mysql-pagamentos-data:/var/lib/mysql
    restart: always

  mysql-pedidos:
    image: mysql:8.0
    container_name: mysql-pedidos
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-changeme}
      MYSQL_DATABASE: alurafood-pedidos
    ports:
      - "3307:3306"
    volumes:
      - mysql-pedidos-data:/var/lib/mysql
    restart: always

  mysql-auth:
    image: mysql:8.0
    container_name: mysql-auth
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-changeme}
      MYSQL_DATABASE: alurafood-auth
    ports:
      - "3308:3306"
    volumes:
      - mysql-auth-data:/var/lib/mysql
    restart: always

  server:
    build: ./server
    container_name: eureka-server
    ports:
      - "9501:9501"
    restart: always

  auth:
    build: ./auth
    container_name: auth-ms
    ports:
      - "9505:9505"
    restart: always
    depends_on:
      - server
      - mysql-auth
    environment:
      SERVER_PORT: 9505
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql-auth:3306/alurafood-auth?createDatabaseIfNotExist=true
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: ${MYSQL_ROOT_PASSWORD:-changeme}
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://server:9501/eureka
      JWT_SECRET: ${JWT_SECRET:-YWx1cmFmb29kLXNlY3JldC1rZXktand0LXNlY3VyaXR5LTI1Ni1iaXRzLW1pbmltdW0=}

  gateway:
    build: ./gateway
    container_name: api-gateway
    ports:
      - "9502:9502"
    restart: always
    depends_on:
      - server
      - auth
    environment:
      SERVER_PORT: 9502
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://server:9501/eureka
      JWT_SECRET: ${JWT_SECRET:-YWx1cmFmb29kLXNlY3JldC1rZXktand0LXNlY3VyaXR5LTI1Ni1iaXRzLW1pbmltdW0=}
      GATEWAY_SECRET: ${GATEWAY_SECRET:-minha-chave-secreta-gateway-2025}

  pagamentos:
    build: ./pagamentos
    container_name: pagamentos-ms
    ports:
      - "9503:9503"
    restart: always
    depends_on:
      - gateway
      - mysql-pagamentos
    environment:
      SERVER_PORT: 9503
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql-pagamentos:3306/alurafood-pagamento?createDatabaseIfNotExist=true
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: ${MYSQL_ROOT_PASSWORD:-changeme}
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://server:9501/eureka
      GATEWAY_SECRET: ${GATEWAY_SECRET:-minha-chave-secreta-gateway-2025}

  pedidos:
    build: ./pedidos
    container_name: pedidos-ms
    ports:
      - "9504:9504"
    restart: always
    depends_on:
      - gateway
      - mysql-pedidos
    environment:
      SERVER_PORT: 9504
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql-pedidos:3306/alurafood-pedidos?createDatabaseIfNotExist=true
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: ${MYSQL_ROOT_PASSWORD:-changeme}
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://server:9501/eureka
      GATEWAY_SECRET: ${GATEWAY_SECRET:-minha-chave-secreta-gateway-2025}

volumes:
  mysql-pagamentos-data:
  mysql-pedidos-data:
  mysql-auth-data:
